{
  "name": "www.jimboche.com",
  "version": "0.0.1",
  "author": "Leon Yu <leon@leonyu.net>",
  "description": "www.jimboche.com",
  "private": true,
  "devDependencies": {
    "cspell": "^9.7.0",
    "html-minifier-terser": "^7.2.0"
  },
  "scripts": {
    "lint": "cspell 'src/**/*.ts' 'public/**/*'",
    "build": "html-minifier-terser --input-dir public/ -c html-minifier.conf --output-dir dist/ && cp -R assets dist"
  }
}
